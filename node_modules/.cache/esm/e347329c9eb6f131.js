let express,dotenv,connectDB;_9eb‍.w("express",[["default",["express"],function(v){express=v}]]);_9eb‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_9eb‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);



// Configuration
dotenv.config();
connectDB();
const app = express();

// Middlewarre
app.use(express.json());

app.get("/", (req, res) => {
   res.send("Server is running");
});

const PORT = process.env.PORT || 5000;

app.listen(5000, () => console.log(`Server is running on port ${PORT}`));
